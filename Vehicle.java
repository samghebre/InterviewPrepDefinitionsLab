package Interface_AbstractClass_ConcreteClass;

public interface Vehicle { // This is interface Vehicle
    default void drive(){ //interface Vehicle can have default method that need to be implement by concrete classes later.
                          // Can only have this method signature

    }
}
